import * as React from 'react';
import {Container} from "@mui/material";
import TextField from "@mui/material/TextField";
import Button from "@mui/material/Button";
/*import {useRef} from "react";*/


const Search = () => {
  /* @todo add functionality */
  /*const addressToSearch = useRef<HTMLInputElement>();
  address = addressToSearch.current?.value*/
  /*
    const form = document.querySelector('#weather-form');
    form.addEventListener('submit', async (event) => {
      //without below we won't see acitiviy in console of browser developer settings
      event.preventDefault();

     // if (!isAuth) return;

      const city = document.getElementById('city').value;

      const res = await axios.get(
        'v1/weather/city',
        {
          params: {
            city,
          },
        },
      );

      document.getElementById("temp-out").innerHTML = `In ${city} sind es ${res.data.temperature} °C`;
      document.getElementById("temp-out").style.display = "block";
    });

    */
  return (
    <div>
      aaaaaaaaaaa
    </div>
  );
};
export default Search;

/*

  <form action="/" method="get">
      <label htmlFor="header-search">
        <span className="visually-hidden">Login first?</span>
      </label>
      <input
        type="text"
        id="address"
        placeholder="Enter address to find place??"
        name="s"
      />
      <button type="submit">Search</button>
    </form>
 */
